<Window x:Class="Actividad_2_MVVM_David_Oviedo.View.Socios"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Actividad_2_MVVM_David_Oviedo.View"
        mc:Ignorable="d"
        Title="Gestión de socios" Height="500" Width="800">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2D6A8D" />
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#1F4A63" />
        <SolidColorBrush x:Key="AccentBrush" Color="#F2B138" />
        <SolidColorBrush x:Key="LightBrush" Color="#F7FAFD" />
        <SolidColorBrush x:Key="BorderBrush" Color="#D6DFE6" />

        <Style x:Key="ValidationStyle" TargetType="Control">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <DockPanel LastChildFill="True">
                            <TextBlock Text="•" Foreground="Red" Margin="0,0,4,0" FontWeight="Bold" />
                            <AdornedElementPlaceholder />
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}" />
                    <Setter Property="BorderBrush" Value="Red" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5" />
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D89C2F" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="GridLinesVisibility" Value="None" />
            <Setter Property="RowBackground" Value="{StaticResource LightBrush}" />
            <Setter Property="AlternatingRowBackground" Value="#E6EEF5" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#EEF3F8" Offset="0" />
                <GradientStop Color="#DCE7F1" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="label_socios" Text="Socios" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryDarkBrush}" Margin="24,12,0,8" />

        <Border x:Name="border_socios" Grid.Row="1" Background="White" CornerRadius="10" Padding="16" Margin="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <GroupBox x:Name="groupbox_socios" Header="Datos del socio">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock x:Name="label_name" Text="Nombre" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8" />
                        <TextBox x:Name="textbox_nombre" Grid.Row="0" Grid.Column="1" Text="{Binding SocioSeleccionado.Nombre, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource ValidationStyle}" />

                        <TextBlock x:Name="label_email" Text="Email" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8" />
                        <TextBox x:Name="textbox_email" Grid.Row="1" Grid.Column="1" Text="{Binding SocioSeleccionado.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource ValidationStyle}" />

                        <TextBlock x:Name="label_activo" Text="Activo" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8" />
                        <CheckBox x:Name="checkbox_activo" Grid.Row="2" Grid.Column="1" IsChecked="{Binding SocioSeleccionado.Activo, Mode=TwoWay}" />

                        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                            <Button x:Name="button_limpiar" Content="Limpiar" Command="{Binding NuevoSocioCommand}" Width="100" Style="{StaticResource PrimaryButton}" />
                            <Button x:Name="button_guardar" Content="Guardar" Command="{Binding GuardarSocioCommand}" Width="100" CommandParameter="{Binding SocioSeleccionado}" Style="{StaticResource AccentButton}" />
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <DataGrid x:Name="dataGrid_socios" Grid.Row="1" ItemsSource="{Binding Socios}" AutoGenerateColumns="False" IsReadOnly="True"
                          SelectedItem="{Binding SocioSeleccionado, Mode=TwoWay}" Margin="0,12,0,0" RowHeight="32">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60" />
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" />
                        <DataGridCheckBoxColumn Header="Activo" Binding="{Binding Activo}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Button x:Name="button_editar" Content="Editar" Command="{Binding GuardarSocioCommand}" Width="100" CommandParameter="{Binding SocioSeleccionado}" Style="{StaticResource PrimaryButton}" />
                    <Button x:Name="button_eliminar" Content="Eliminar" Command="{Binding EliminarSocioCommand}" Width="100" CommandParameter="{Binding SocioSeleccionado}" Style="{StaticResource PrimaryButton}" Background="{StaticResource PrimaryDarkBrush}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
